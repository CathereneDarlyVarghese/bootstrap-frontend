{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "build": {
          "env": {
            "BUILD_COMMAND": "npm install --force && npm run build"
          }
        }
      }
    }
  ]
}
